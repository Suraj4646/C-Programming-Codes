#include<stdio.h>
int main()
{
	int n;
	printf("enter the value of array : ");
	scanf("%d",&n);
	int a[n],i,j,c=0;
	for(int i=0;i<n;i++)
	scanf("%d",&a[i]);
	for(int i=0;i<n;i++)
	printf("%d",a[i]);
	for(int i=0;i<n;i++)
	int f[i],f[j];
	{
		if(f[i]==0)
		int c=0;
		for(int j=0;j<n;j++)
		{
			if(a[i]==a[j])
			f[j]==1;
			c++;
			printf("%d occurs %d times",a[i],c);
			return 0;
					}
	}
}